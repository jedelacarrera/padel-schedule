steps:
  - id: python-builder
    name: "gcr.io/cloud-builders/gcloud"
    waitFor:
      - "-"
    args:
      - builds
      - submit
      - front
      - --config=python-cloud-builder/cloudbuild.yaml

timeout: 900s
